
## Durable updates from 2026-04-08

- Open Brain recovery completed and verified: restored `openbrain-*` scripts, rebuilt `.venv`, reinstalled dependencies, recreated `.env`, and confirmed PostgreSQL + pgvector + Ollama embeddings work end-to-end.
- Weekly backup now includes:
  - critical workspace state
  - `.env`
  - workspace SSH keypair
  - compressed PostgreSQL dump of the `openclaw` database
  - retention of newest 3 copies for both filesystem snapshots and DB dumps
- GitHub push access restored with a new workspace deploy key in `.ssh/id_ed25519`; repository connectivity verified and recovery work pushed.
- Native OpenClaw scheduled jobs restored for RSS/news/stock/Goodreads/maintenance/backup; old system crontab entries removed afterward.
- Guido van Rossum site has a real RSS feed at `https://gvanrossum.github.io/interviews/feed.xml`; added to `rss-feeds.opml`; no separate Guido website check should be used.
- Linkding tracking upgraded to write recommendation signals to `data/linkding-recommendation-signals.json` and semantic summaries into Open Brain when new `toread` items appear.
- Linkding tracker first backfill on 2026-04-08 synced 3662 historical `toread` bookmarks and created Open Brain memory object #6.
- Todoist: native root config `integrations.todoist` is NOT supported by installed OpenClaw 2026.3.13 (`<root>: Unrecognized key: "integrations"`).
- Installed `openclaw-todoist-plugin` from npm (plugin id `todoist`) and installed `@doist/todoist-cli` (`td` 1.39.3). Plugin configured via `plugins.entries.todoist.config.apiToken`; gateway restart/test was in progress when compaction flush happened.
- Multiple secrets/tokens were pasted in Discord during recovery/troubleshooting (Postgres password, Linkding API key, Todoist token); these should be rotated when convenient.
- Chris's Discord user ID: `373646860591693828`.

- `pyatari` Phase 3 completed and pushed as commit `559e273` (`Implement Phase 3 CPU scaffolding`): opcode table, addressing resolver, CPU skeleton, disassembler, and focused tests.
- `pyatari` Phase 4 completed and pushed as commit `89b8063` (`Implement Phase 4 CPU execution and Klaus validation`): full official 6502 execution, additional execution tests, Klaus Dormann functional test ROM/harness, and BRK return-address fix discovered by Klaus.
- Klaus 6502 functional test reaches success loop at PC `0x3469` under `pyatari`; run is intentionally opt-in slow (`PYATARI_RUN_KLAUS=1`) because it takes about 30.6M instructions / 95.9M cycles / ~51s in Python.
- Likely OpenClaw Discord regression observed: native status/usage-style commands in Discord appear flaky/misdelivered despite gateway health being fine; probable issue is Discord native interaction handling rather than auth/config basics.
- Evening news digest behavior is important: use `scripts/news-digest.py` (not `news-digest.sh`) and preserve coverage markers exactly as produced: `🟪 🟥 🟦 ⚖️`. Do not paraphrase away the legend or marker labels in delivery.
- When confirming RSS/article saves to Linkding, always include the article titles alongside the saved item numbers/IDs so Chris can verify the right items were saved.
- Chris preference: when he asks for reminders, save them to Todoist.

- `pyatari` Phase 5 completed and pushed as commit `ebabe46` (`Implement Phase 5 clock and machine loop`): added `clock.py`, initial `machine.py`, CPU `irq()/nmi()` handling, machine execution helpers, and clock/machine interrupt tests.
- `pyatari` regression test added and pushed as commit `739a5e4` (`Add INX execution regression test`): verifies `INX` executed 20 times from X=0 leaves X at 20. Chris said "INCX" but meant real 6502 `INX`.
- `pyatari` Phase 6 completed and pushed as commit `5b81d4d` (`Implement Phase 6 PIA chip`): added `pia.py`, DDR/data register behavior, joystick input handling, PORTB bank switching wiring, register mirroring, machine integration, and PIA tests.
- `pyatari` Phase 7 was implemented but not yet committed at flush time: added initial `antic.py` core with ANTIC register handlers, display list parsing (`JMP`, `JVB`, blank/mode lines, LMS/DLI/HSCROL/VSCROL flags), `VCOUNT`, `WSYNC`, `NMIST` VBI/DLI status, machine ANTIC integration, and `tests/test_antic.py`.

- `pyatari` Phase 7 completed and pushed as commit `60243ae` (`Implement Phase 7 ANTIC core`): added `antic.py`, ANTIC register handling, display-list parsing, scanline stepping, `VCOUNT`, `WSYNC`, `NMIST` VBI/DLI status, machine ANTIC timing/NMI integration, and ANTIC tests.
- `pyatari` Phase 8 completed and pushed as commit `682d00e` (`Implement Phase 8 GTIA rendering core`): added `gtia.py` and `display.py`, wired `GTIA` and `DisplaySurface` into `machine.py`, exported both, added GTIA tests, and implemented Atari color-byte to RGB mapping plus initial ANTIC mode 2 text rendering.
- `pyatari` Phase 9 completed and pushed as commit `c7fde04` (`Implement Phase 9 ANTIC mode rendering`): expanded GTIA rendering to ANTIC modes `2-7` and `8-F`, added `CHBASE`/`CHACTL` behavior including inverse/reflect handling and 20-column double-width text, updated machine wiring, and expanded GTIA tests.
- `pyatari` Phase 10 completed and pushed as commit `4df3ac3` (`Implement Phase 10 POKEY timers and IRQs`).
- `pyatari` Phase 13 completed and pushed as commit `95e708e` (`Implement Phase 13 SIO and loaders`): added `sio.py` with ATR parsing, `DiskDrive`, `SIOBus`, XEX parsing/loading, boot-sector exposure, machine disk/XEX loading hooks, and `tests/test_sio.py`; validation reached `105 passed, 1 skipped`.
- `pyatari` Phase 14 completed and pushed as commit `c56c5e9` (`Implement Phase 14 debugger`): added `debugger.py` with register snapshots, trace history, step/continue, breakpoints, basic watchpoints, disassembly and memory dump views, chip-state summary, and dedicated debugger tests; validation reached `110 passed, 1 skipped`.
- Chris asked for more in-progress narration while work is underway; provide short, concrete milestone updates instead of going quiet for long stretches.
- Todoist reminder workflow repaired enough to work again: reminder created for 2026-04-09 to review planning docs/PLANs in `https://github.com/dagster-io/erk/tree/master`.
- Todoist CLI auth detail: plugin token existed in OpenClaw config, but `td` itself lacked credentials and had to be re-synced; `td` warned that the system credential manager was unavailable and stored plaintext config in `~/.config/todoist-cli/config.json`.
- Workspace repo hygiene lesson: when operational reality changes, update the GitHub-backed workspace repo promptly so repo docs and long-term memory do not drift stale. Chris explicitly wants the repo kept current and pushed regularly.
- Stale repo memory corrected: current OpenClaw runtime model is `openai-codex/gpt-5.4`, not the older Claude Sonnet note that had been left in `MEMORY.md`.
- Added Todoist durability assets to the workspace repo: `scripts/fix-todoist-auth.sh`, `docs/todoist-recovery.md`, and backup coverage for `~/.openclaw/openclaw.json` plus `~/.config/todoist-cli/config.json`; verified by a real backup run at `critical-state-20260408T201512Z`.
