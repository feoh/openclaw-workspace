# Daily Notes — 2026-04-17

- RSS digest backfill was extended in `scripts/rss-digest.py` to include Lobsters alongside Ars Technica. Current bounded backfill settings apply to both feeds: up to 50 entries within the last 60 days. Commit pushed: `f2f128d` (`Extend RSS backfill to Lobsters`).
- OpenClaw web search was configured to use Perplexity on this machine. The correct config path is `plugins.entries.perplexity.config.webSearch`, and provider selection belongs under `tools.web.search.provider`.
- During Perplexity setup, a schema mismatch was caught and corrected: provider selection is not top-level `web.search`; using that shape would be invalid.
- Result of validation after the fix: the bundled `perplexity` plugin was enabled, config validated, and it advertised `webSearchProviderIds: ["perplexity"]`.
