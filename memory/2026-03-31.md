# Daily Notes — 2026-03-31

## Model decision
Chris decided to keep using `anthropic/claude-sonnet-4-6` as the primary model.
- Tested Qwen2.5 7B locally (pulled, benchmarked)
- Decision: DO NOT switch to Qwen or any local model
- Current model: `anthropic/claude-sonnet-4-6` — keep this
- Note: model config in `~/.openclaw/openclaw.json` → `agents.defaults.model.primary`

## Evening News Digest Improvements
- Rebuilt news-digest.py: cross-spectrum story grouping (🟪 both sides · 🟥 · 🟦 · ⚖️)
- Top 5 stories only, ranked by source count
- Added retry logic (3 attempts, 2s backoff per source)
- Minimum 2-source threshold — single-source stories excluded
- Fixed broken feeds: Reuters (dead 2020), AP News (bad XML), MSNBC (dead URL)
  → Replaced with NBC News, The Hill, Politico (all verified working)
- Error reporting: warnings surface in digest output + cron notifies on failure
- Spec saved to: docs/evening-news-spec.md (CHECK THIS before changing news digest)

## Model / reliability notes
- Anthropic API overload errors seen today (their end, not ours)
- Decision: keep Claude Sonnet-4.6, do NOT switch to Qwen or local models

## Reliability note
- Chris is on a paid Anthropic API plan — NOT free tier
- Anthropic 529 overload errors are surfaced to user rather than retried
- This is unacceptable — silent failures and dropped requests are not OK
- OpenClaw doesn't expose retry config in openclaw.json — this is a gateway-level behavior
- TODO: raise with OpenClaw devs as feature request (retry with backoff on 529)
- Workaround: Chris resends message if he sees the error
