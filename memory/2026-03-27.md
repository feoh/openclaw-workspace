# Daily Notes — 2026-03-27

## Session 1 (morning)

### Open Brain Memory System — Built from spec
Chris provided a spec document for a database-backed memory layer. Built complete system:

**Infrastructure:**
- PostgreSQL 16.13 + pgvector 0.6.0 on localhost:5432
- Database: `openclaw`, User: `simplificus`, Password: in `.env`
- Ollama `nomic-embed-text` model (768-dim embeddings)

**Scripts created:**
- `openbrain-schema.py` — creates memory_objects table with all spec fields
- `openbrain-write.py` — save with auto-embedding via Ollama
- `openbrain-search.py` — keyword + semantic (vector similarity) search
- `openbrain-promote.py` — curation (private→controlled→public lanes)
- `openbrain-health.py` — JSON health snapshot to `data/open_brain_health.json`
- `openbrain-mcp.py` — MCP server for external tool integration

**Schema fields:** id, title, summary, body, lane, obj_type, domain_tags, provenance, confidence, source_links, embedding, created_at, updated_at, freshness_ts, curated, promoted_at

**Spec compliance:** private-first lanes, explicit promotion, health snapshots, fail-open base memory

### RSS Feed Checker fix
- Changed from auto-save-everything to selective report workflow
- Script reports numbered list → user says "save #X" → I save to Linkding

### Misc
- AMZN stock check cron created (daily 6 AM EDT)
- Updated IP assignments in Obsidian (removed defunct VMs: zima, quartermaster, distra, retrodev)
- Documented Cookie photo in assets/

## Session 2 (evening)

### RSS saves — Chris said "save 1,9,1011,12,17,1826,57,58,5974,75,77,79,83"
Parsed concatenated numbers correctly:
- 1011 = #10 + #11
- 1826 = #18 + #26
- 5974 = #59 + #74

Saved 13 articles to Linkding (IDs 6246-6273) with `toread` tag.

### All commits pushed
- `b2100dd` — Open Brain: semantic search with nomic-embed-text
- `961d054` — Open Brain: health snapshot, promotion workflow
- `50b8939` — Open Brain: MCP server for external tool integration
- `ae4dfc3` — Memory: Open Brain system built, session summary
- `3de96e3` — Save health snapshot, rss state

## Open Brain Memory System (complete)
Full stack now operational:
- PostgreSQL + pgvector database
- Ollama nomic-embed-text for embeddings
- 6 Python tools for CRUD + health
- MCP server for external app integration
- Health snapshot cron-compatible

Chris is enthusiastic — mentioned starting on 8-bit Atari 400 with 16K RAM and cassette drive. Finds AI agents "fascinating."
