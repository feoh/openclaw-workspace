# 2026-04-15

- Chris called out another regression in RSS save confirmations. Durable rule: whenever confirming RSS saves to Linkding, always include the article titles alongside the saved item numbers and IDs. Do not reply with IDs/numbers alone.
- New durable Python rule: always use the workspace virtualenv interpreter for workspace tasks (`/home/feoh/.openclaw/workspace/.venv/bin/python`), leave system Python untouched, and manage workspace dependencies with `uv`.
