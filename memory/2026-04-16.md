- Blind Not Dumb Hugo migration reached working state on `main`: live `https://www.feoh.org/rss.xml` and `https://www.feoh.org/feed.atom` both eventually updated correctly after deploy/invalidation propagation.
- Root cause of stale Atom during Blind Not Dumb deploy debugging: `blindnotdumb/scripts/generate_legacy_feeds.py` had a hardcoded Linux workspace path; fixed on upstream `main` in commit `e15e771` (`Fix feed generator to use repo-relative paths`).
- Chris asked to do the same Hugo conversion for personal blog `https://github.com/feoh/playing-with-quicksilver`.
- For `playing-with-quicksilver`, work is intentionally staying on separate preview branch `hugo` so Chris can review before any final merge/deploy decision.
- `playing-with-quicksilver` initial Hugo migration branch was started and pushed; later polish push on `hugo` used commit `3ea843c` (`Polish Hugo preview layouts`).
- Current `playing-with-quicksilver` Hugo preview status: Hugo scaffold exists, Nikola posts converted into `content/posts/`, images copied into `static/images/`, homepage/single/list/tag templates added, and template summary rendering was cleaned up so homepage/archive excerpts are reviewable.
