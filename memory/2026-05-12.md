# 2026-05-12

- Fixed another Politico regression in `scripts/news-digest.py`. The failure mode had changed from malformed XML to `HTTP 403` from Cloudflare when using minimal urllib headers.
- Updated the feed fetcher to fall back to a browser-like header profile after the lightweight RSS request profile fails.
- Verified the evening digest succeeds again and Politico stories appear in live output.
- Chris explicitly asked for two durable workflow rules: (1) commit and push fixes by default so they stick, and (2) give more progress updates during longer tasks instead of going silent.
