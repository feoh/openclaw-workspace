# 2026-05-08

- Investigated recurring Politico failures in `scripts/news-digest.py` after Chris reported daily issues.
- Confirmed from cron history that Politico had been intermittently failing since mid/late April with XML parse errors like `not well-formed (invalid token)`; some days it worked, some days it didn't.
- Live checks showed `https://www.politico.com/rss/politicopicks.xml` is still the right feed URL, but direct browser-like fetches can hit Cloudflare and the feed content itself is sometimes malformed XML.
- Hardened `scripts/news-digest.py` to fetch RSS with explicit headers, parse raw bytes directly, and retry with XML sanitization (strip illegal XML chars, escape bare ampersands) when a source returns malformed XML.
- Verified the digest now pulls Politico successfully again, with Politico stories appearing in output.
- Upgraded RSS recommendation training/scoring (Phase 1): `scripts/track-linkding.sh` now keeps a larger Linkding history, sorts bookmarks by actual save time, builds recency-weighted signals (400-item recent window, 180-day half-life), and writes weighted domains/sites/title terms/bigrams/feed terms.
- Upgraded `scripts/rss-digest.py` to consume weighted signals, compute a continuous recommendation score with human-readable reasons, and star articles using weighted recent preferences instead of the old binary set-overlap heuristic.
- Verified the refreshed digest now produces scored/starred recommendations grounded in recent Linkding saves rather than old 2012-era bookmarks.
